name: hack20
description: Chat app with 90s IRC inspired interface for Hack20.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_treeview: ^0.4.1+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter95: ^0.0.7

flutter:

  uses-material-design: true
